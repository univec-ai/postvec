{
  "name": "postvec.dev",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Documentation site for postvec — semantic columns for PostgreSQL",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.21"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
