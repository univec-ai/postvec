.DS_Store
.idea/
/target
*.iml
**/*.rs.bk
# Cargo.lock is committed (inherited pgrx *library* templates ignore it, but
# this crate ships a production .so loaded via shared_preload_libraries —
# release artifacts must be reproducible and transitive bumps deliberate).
tests/pg_regress/results
tests/pg_regress/regression.diffs
tests/pg_regress/regression.out
